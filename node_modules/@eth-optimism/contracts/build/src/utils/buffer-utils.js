"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getHexSlice = exports.toHexString32 = exports.fromHexString = exports.toHexString = void 0;
const ethers_1 = require("ethers");
exports.toHexString = (buf) => {
    return '0x' + exports.fromHexString(buf).toString('hex');
};
exports.fromHexString = (str) => {
    if (typeof str === 'string' && str.startsWith('0x')) {
        return Buffer.from(str.slice(2), 'hex');
    }
    return Buffer.from(str);
};
exports.toHexString32 = (input, padRight = false) => {
    if (typeof input === 'number') {
        input = ethers_1.BigNumber.from(input).toHexString();
    }
    input = exports.toHexString(input).slice(2);
    return '0x' + (padRight ? input.padEnd(64, '0') : input.padStart(64, '0'));
};
exports.getHexSlice = (input, start, length) => {
    return exports.toHexString(exports.fromHexString(input).slice(start, start + length));
};
//# sourceMappingURL=buffer-utils.js.map